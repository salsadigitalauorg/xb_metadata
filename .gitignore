vendor

# Ignore everything in web by default
web/*

# Allow modules and themes directories (structure)
!web/modules/
!web/themes/

# Ignore all module content by default
web/modules/*

# Allow custom modules directory (recursively)
!web/modules/custom/

# Track all themes (recursively)
!web/themes/**
