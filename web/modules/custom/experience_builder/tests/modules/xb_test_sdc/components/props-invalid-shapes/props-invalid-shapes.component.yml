$schema: https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json
name: XB test SDC with invalid shape
props:
  type: object
  required:
    - heading
  properties:
    # "Object" prop-types are not supported by XB.
    invalid_shape:
      title: Invalid Shape
      type: object
    valid_shape:
      $ref: json-schema-definitions://experience_builder.module/image
      title: Valid Shape
      type: object
