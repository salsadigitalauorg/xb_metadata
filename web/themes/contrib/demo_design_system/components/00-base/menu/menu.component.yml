'$schema': 'https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json'

name: Menu

status: obsolete

group: Base

props:

  type: object
  # required:
  #   - items
  #     - title
  #     - url

  properties:
    theme:
      type: string
      title: Theme
      description: 'Theme: light, dark.'
      default: light
      enum:
        - light
        - dark
      examples:
        - light

    # @fixme TypeError: Drupal\experience_builder\PropShape::resolveSchemaReferences(): Argument #1 ($schema) must be of type array, string given
    # @todo add examples to these when updating.
    #items:
    #type: array
    #title: Items
    #description: 'Menu links array. Each item contains:'
    #items:
    #type: object
    #
    #properties:
    #title:
    #type: string
    #title: Title
    #description: The title of the link.
    #
    #url:
    #type: string
    #title: Url
    #description: The link url.
    #
    #below:
    #type: array
    #title: Below
    #description: Array of submenu items.
    #
    #is_expanded:
    #type: boolean
    #title: Is expanded
    #description: Flag if the current item has visible child.
    ##
    ##in_active_trail:
    #type: boolean
    #title: In active trail
    #description: Flag if the current item is in the active trail.

    is_collapsible:
      type: boolean
      title: Flag whether to collapse the menu.
      examples:
        - false

    modifier_class:
      type: string
      title: Modifier class
      description: Additional classes.
      examples:
        - 'custom-class'
